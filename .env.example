# Environment Variables for ReadRitual Development

# PostgreSQL Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Rails Configuration
RAILS_ENV=development

# Database URL (automatically constructed in workflows)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/read_ritual_development

# For devcontainer/docker-compose development:
# Copy this file to .env and modify values as needed
# The devcontainer will use these values if provided

# Security Note:
# For GitHub Actions workflows, use GitHub repository secrets instead
# of hardcoding credentials. Set POSTGRES_PASSWORD as a repository secret.